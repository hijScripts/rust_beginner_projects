fn main() {
    println!("Hello, world!");
}

// compile: cargo build
// execute: ./target/debug/[cargo dir name]

// compile + run: cargo run

// compile w/out executable: cargo check

// build for release: cargo build --release (goes to target/release)